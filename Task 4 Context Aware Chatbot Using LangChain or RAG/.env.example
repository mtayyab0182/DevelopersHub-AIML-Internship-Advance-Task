# Copy this file to .env and fill in your key if you want to use the OpenAI backend.
# Not required if you use --llm_backend local (default) — that runs fully offline
# after the model is first downloaded.

OPENAI_API_KEY=your_openai_api_key_here
